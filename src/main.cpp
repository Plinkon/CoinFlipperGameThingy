#include <iostream>
#include <vector>

class gameState {
    private:
        float points;
        std::vector<const char*> packsInInventory;
    public:
        gameState() = default;
        ~gameState() = default;

    // todo: add get and set methods
};

int main() {
    // todo: start game stuff and init settings


    while (true) {
        // todo: show shop and inv.

        // todo: open packs and flip coins
        
    }
}